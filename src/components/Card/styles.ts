import styled from "styled-components";

export const Container = styled.div`
  background-color: var(--card-bg);
  padding: 1.5rem;
  border-radius: 12px;

  .nft-img {
    aspect-ratio: 1;
    height: 280px;
    border-radius: 8px;
  }
`;
